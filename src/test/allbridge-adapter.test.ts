import { describe, it } from 'vitest';

describe('allbridge-adapter', () => {
  it('placeholder — integration tests require live SDK', () => {
    // Allbridge SDK integration tests require a live network connection.
    // Unit-level coverage is provided via the route-level mocks in bridge-build-tx.test.ts.
  });
});
